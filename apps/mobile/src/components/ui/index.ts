/**
 * Componentes visuales compartidos.
 *
 * Se importan desde `../components/ui` y no de a uno para que sumar un
 * componente nuevo no obligue a tocar los imports de cada pantalla.
 */
export { Button, type ButtonVariant } from './Button';
export { CodeDisplay } from './CodeDisplay';
